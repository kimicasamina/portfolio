name: Auto Deploy Self-Hosted Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Sync to  webserver
        run: |
          mkdir -p ~/webserver/apps/portfolio
          rsync -av --delete ./ ~/webserver/apps/portfolio

      - name: Deploy containers
        run: |
          cd ~/webserver
          docker compose down
          docker compose up -d --build